1. Cài thư viện:
   pip install -r requirements.txt

2. Tạo file .env chứa:
   OPENWEATHER_API_KEY=YOUR_KEY

3. Chạy chương trình:
   streamlit run app.py

4. Dữ liệu sử dụng:
   hanoi_weather.csv

5. Mô hình:
   - SARIMA (SARIMAX) cho chuỗi thời gian
   - Random Forest cho mô hình ML
